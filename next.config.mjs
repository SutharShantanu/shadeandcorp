/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["picsum.photos", "img.freepik.com", "via.placeholder.com" , "assets.myntassets.com"],
  },
};

export default nextConfig;
